# ---- Build intermediates (safe to regenerate) ----
[Bb]in/
[Oo]bj/
*.dll.mdb
*.pdb

# ---- Visual Studio / Rider / user-specific ----
.vs/
.idea/
*.user
*.suo
*.userprefs
*.cache
*.tmp

# ---- OS cruft ----
Thumbs.db
Desktop.ini
.DS_Store

# ---------------------------------------------------------------------------
# INTENTIONALLY TRACKED (do NOT ignore) — these are preservation artifacts:
#   ReleaseArchive/*.zip   -> versioned built packages that travel with repo
#   rollback/**            -> preserved rollback DLL/version/README snapshots
#   *.csproj, *.cs, README.txt, ROLLBACK.md, RELEASE_PROCESS.md, *.version
# ---------------------------------------------------------------------------
